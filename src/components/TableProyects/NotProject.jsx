import { Typography } from '@mui/material';

const NotProject = () => {
  return (
    <Typography sx={{
      width: '100%',
      textAlign: 'center',
      padding: '50px',
      background: '#fff',
    }}>
      Not projects
    </Typography>
  );
};

export default NotProject;
